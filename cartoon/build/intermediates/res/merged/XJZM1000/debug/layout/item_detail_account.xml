<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:descendantFocusability="blocksDescendants"
    android:paddingBottom="16dp"
    android:paddingLeft="10dp"
    android:paddingRight="10dp"
    android:paddingTop="16dp">

    <LinearLayout
        android:id="@+id/layout_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="4000杯账号"
            android:textColor="#333333"
            android:textSize="@dimen/text_size_medium_small" />

        <TextView
            android:id="@+id/tv_state"
            android:layout_width="@dimen/btn_account_state_width"
            android:layout_height="@dimen/btn_account_state_height"
            android:layout_marginLeft="10dp"
            android:background="@drawable/game_account_free_state_bg"
            android:gravity="center"
            android:text="空闲"
            android:textColor="@android:color/white"
            android:textSize="@dimen/topgame_account_state" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_intro"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@id/layout_title"
        android:layout_below="@id/layout_title"
        android:layout_marginTop="6dp"
        android:text="全传奇卡版完整体验"
        android:textColor="#666666"
        android:textSize="@dimen/text_size_small" />

    <Button
        android:id="@+id/btn_play"
        android:layout_width="@dimen/btn_play_width"
        android:layout_height="@dimen/btn_play_height"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:background="@drawable/play_directly_enable_true_bg"
        android:text="直接玩"
        android:textColor="#ffffff"
        android:textSize="13sp" />

</RelativeLayout>